<p style="color: green"><%= notice %></p>

<h1>Creador de Categorías</h1>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
    <p>
      <%= link_to "Show this category type", category %>
    </p>
  <% end %>
</div>

<%= link_to "New category", new_category_path %>

